# Fluxy Desktop

Fluxy Desktop is the local-first app shell.

It reuses the web interface and runs a local FastAPI sidecar for database connections, credentials, backups, sandboxes, synthetic data and MCP.

Desktop must remain fully functional without login. Login only enables cloud sync.

